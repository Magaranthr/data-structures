import java.util.Stack;

/**
 * This program simulates an undo stack. Note that operations
 * must be undone in the opposite order in which they are first
 * issued.
*/
// lets play a game where letters ar einput and every other letter is given in a separat eline them meshed together at the end without once reading the word. If the input was noipgegtefr 

public class StackDemo
{
    public static void main(String[] args)
    {
        // Create an empty stack.
        Stack<String> undoStack = new Stack<String>();
    }
}
